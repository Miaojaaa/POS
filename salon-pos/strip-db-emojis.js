const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); const emojiRegex = /[\u{1F300}-\u{1F64F}\u{1F680}-\u{1F6FF}\u{2600}-\u{26FF}\u{2700}-\u{27BF}\u{1F900}-\u{1F9FF}\u{1F1E6}-\u{1F1FF}\u2728\u2B50\uFE0F]/gu; async function run() { console.log('Starting DB emoji strip...'); const strip = s => s.replace(emojiRegex, '').trim(); let count = 0; const groups = await prisma.serviceGroup.findMany(); for (const g of groups) { const cleaned = strip(g.name); if (cleaned !== g.name) { await prisma.serviceGroup.update({ where: { id: g.id }, data: { name: cleaned } }); count++; } } const cats = await prisma.category.findMany(); for (const c of cats) { const cleaned = strip(c.name); if (cleaned !== c.name) { await prisma.category.update({ where: { id: c.id }, data: { name: cleaned } }); count++; } } const svcs = await prisma.service.findMany(); for (const s of svcs) { const cleaned = strip(s.name); if (cleaned !== s.name) { await prisma.service.update({ where: { id: s.id }, data: { name: cleaned } }); count++; } } console.log('Done! Updated ' + count + ' records.'); await prisma.$disconnect(); } run().catch(console.error);
